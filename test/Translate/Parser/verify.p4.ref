match_kind {
    exact,
    ternary,
    lpm
}

error {
    NoError,
    SomeError
}
@corelib() extern void verify(in bool check, in error toSignal);

parser p2(in bool check, out bool matches) {
    state start {
        verify((check == true), error.SomeError);
        transition next;
    }
    state next {
        matches = true;
        transition accept;
    }
}
