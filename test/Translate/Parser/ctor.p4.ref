parser p(in int<10> sinit)(bool ctorval) {
    int<10> s = (ctorval ? (10s0) : (sinit));

    state start {
        s = (int<10>)(10s1);
        transition next;
    }
    state next {
        s = (int<10>)(10s2);
        transition accept;
    }
    state drop {
        transition reject;
    }
}
