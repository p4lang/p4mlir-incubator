match_kind {
    exact,
    ternary,
    lpm
}

header MyHeader {
    int<16> f1;
}
enum action_run_0 {
    foo,
    bar,
    bak
}
extern Ext {
    Ext(int<16> tmp);
}

extern int<16> baz(in int<16> arg);

action bak() {
    return;
}

control Pipe(in MyHeader arg1, in int<16> arg2, inout int<16> arg3)(int<16> ctr_arg1) {
    MyHeader local_hdr = (MyHeader) {f1 = 16s23};
    int<16> test = (int<16>)(16s52);

    action foo(in int<16> x1, inout int<16> x2) {
        return;
    }
    action bar(int<16> x1, int<16> x2, int<16> x3) {
        local_hdr.f1 = arg2;
        return;
    }
    table myTable {
        key = {
            (arg1.f1 - 16s1) : exact @name("arg1.key") ;
            (arg3 & 16s43707) : lpm @name("arg3.key") ;
            16s3 : lpm @name("cst.key") ;
            baz(test) : exact @name("baz.key") ;
            local_hdr.f1 : lpm @name("local_hdr.key") ;
        }
        actions = {
            foo(arg2, arg3);
            bar;
            bak;
        }
        default_action = bak();
    }
    apply {
        myTable.apply();
        test = (test + 16s1);
        local_hdr.f1 = (int<16>)(16s3);
    }
}
